import XCTest
@testable import AnimatedTextField

final class AnimatedTextFieldTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AnimatedTextField().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
